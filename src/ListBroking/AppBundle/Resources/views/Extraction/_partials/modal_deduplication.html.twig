<div class="modal fade" id="lead_deduplication_modal">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span
                            class="sr-only">Close</span>
                </button>
                <h4 class="modal-title"><i class="fa fa-times"></i>&nbsp;Lead Deduplication</h4>
            </div>
            <div class="modal-body">
                <p>As a security measure, the extraction can only be sent to the <b>its Creator</b> and the file will be <b>zipped</b> with a password.</p>
                <blockquote class="small-label">
                    <div class="form-group">
                        <label>Download file <small>(Excel file with contact information for deduplication)</small></label>
                        <input type="hidden" data-select-mode="ajax" data-select-minimum-input="0" data-select-type="ExtractionTemplate" data-placeholder="Select a template..." class="form-control
                        row-margin" value="1" disabled="disabled">
                        <button type="button" id="deduplication-download" data-extraction="{{ extraction.id }}" class="btn btn-flat btn-primary space"><i class="icon icon-large ion-loading-c" style="display: none;
                        "></i>&nbsp;Extract Leads List</button>
                    </div>
                    <small>Extraction will be sent to: <b>{{ app.user.email }}</b></small>
                </blockquote>
                </div>
            {{ form_start(forms.extraction_deduplication, {attr: {'data-extraction': extraction.id}}) }}
            <div class="modal-body">
                <blockquote class="small-label">
                    {{ form_row(forms.extraction_deduplication.deduplication_type) }}
                    {{ form_row(forms.extraction_deduplication.field) }}
                    {{ form_label(forms.extraction_deduplication.upload_file) }}
                    <span class="btn btn-flat btn-primary fileinput-button form-control">
                        <i class="glyphicon glyphicon-plus"></i>
                        <span>Select file...</span>
                        <!-- The file input field used as target for the file upload widget -->
                        {{ form_widget(forms.extraction_deduplication.upload_file) }}
                    </span>
                    <small><strong id="files">No file chosen</strong></small>
                    <small>CSV file with duplicated contacts (one column <strong>only</strong>)</small>
                    <small id="fileuploaderror" class="text-danger" style="display: none;"></small>

                    <div class="progress hidden">
                        <div class="progress-bar progress-bar-success"></div>
                    </div>
                </blockquote>
            </div>

            <div class="modal-footer">
                <button id="extraction_deduplication_upload_button" type="button" class="btn btn-flat btn-primary">
                    <i class="fa fa-filter"></i>&nbsp;Deduplicate&nbsp;
                    <i class="loading ion-loading-c" style="display: none;"></i>
                </button>
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            </div>
            {{ form_rest(forms.extraction_deduplication) }}
            {{ form_end(forms.extraction_deduplication) }}
        </div>
        <!-- /.modal-content -->
    </div> <!-- /.modal-dialog -->
</div><!-- /.modal -->